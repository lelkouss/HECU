function setup() {
  width = 500
  height = 500
  createCanvas(width, height);
  colorMode(HSB, 360, 100, 100);
  fill(30,100,100);
  // setting up canvas
}

function draw() {
  background(bg_color);
}